/*
32. Задание на МС.  Напишите объект, описывающий модель телефона,
 заполнив все свойства значениями, прочитанными из prompt
 (например: brand, model, resolution, color...).
*/
var smartphone = {
    brand:null,
    model:null,
    resolution:null,
    color:null,
    battery:null,
    size:null};

for (x in smartphone){
    var obj = new smartphone( var x = prompt('Input '+ x)
}

